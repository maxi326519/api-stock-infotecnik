import { PriceDetail, SaleDetail, SaleInvoice } from "../../interfaces/Sales";

export function createRectifyInvoicePDF(sale: SaleInvoice, details: SaleInvoice, prices: PriceDetail ){
  return "nueva factura";
}